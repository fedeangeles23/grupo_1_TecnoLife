<!DOCTYPE html>
<html lang="es">

<%- include('./partials/head') %>

    <body>

        <!-- header section start  -->

        <%- include('./partials/header') %>

            <main class="home buscador" >

                <h3 class="welcome">Resultado de <span class="span-color">Su Búsqueda</span></h3>
                <section>
                    <div class="article">
                        <% productos.forEach(producto=> { %>
                            <div class="card swiper-slide">

                                <div class="img-product">
                                    <a href="/products/detail/<%= producto.id %> ">
                                        <img class="item" src="/img/products/<%= producto.imagenes[0].nombre%>"
                                            alt="Notebook-spin">
                                    </a>
                                </div>

                                <div class="show-details">
                                    <span class="show_name">
                                        <%= producto.nombre %>
                                    </span>
                                    <p>
                                        <% if (producto.descripcion.length> 50) { %>
                                            <%- `${producto.descripcion.slice(0,150)}...` %>
                                                <% } else { %>
                                                    <%= producto.descripcion %>
                                                        <% } %>
                                    </p>
                                </div>

                                <div class="color-price">

                                    <div class="color-option">
                                        <span class="color">Precio:</span>
                                    </div>

                                    <div class="price">
                                        <span class="price_num">$<%=producto.precio%></span>
                                        <span class="price_letter">Precio lista</span>
                                    </div>

                                </div>

                                <div class="button">
                                    <div class="button-layer"></div>
                                    <button><a href="/carrito"></a>Añadir al carrito</button>
                                </div>

                                <div class="button">
                                    <div class="button-layer"></div>
                                    <a href="/products/detail/<%= producto.id %>"><button>Ver Detalles</button></a>
                                </div>
                            </div>
                        <% }) %>

<!--                         <div class="swiper-button-next swiper-navBtn"></div>
                        <div class="swiper-button-prev swiper-navBtn"></div>
                        <div class="swiper-pagination"></div> -->
                            <!--     </div>
                        </div>
                    </div> -->
                    </div>
                </section>
            </main>

            <%- include('./partials/footer') %>

    </body>

</html>